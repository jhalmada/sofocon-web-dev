const CompButtonCancel = ({ text }) => {
  return (
    <button className="flex h-[40px] items-center justify-center">
      <h2 className="text-sm font-medium leading-[24px] text-black_b">
        {text}
      </h2>
    </button>
  );
};

export default CompButtonCancel;
