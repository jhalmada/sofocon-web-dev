const CompButtonCancel = ({ text }) => {
  return (
    <button className="flex h-10 items-center justify-center">
      <h2 className="text-sm font-medium leading-6 text-black_b">{text}</h2>
    </button>
  );
};

export default CompButtonCancel;
